// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.ts`, but if you do
// `ng build --env=prod` then `environment.prod.ts` will be used instead.
// The list-categories of which env maps to which file can be found in `.angular-cli.json`.

export const environment = {
    production: false,
       apiUrl: 'https://api.showcasesweb.com/api/',
    //  apiUrl: 'http://127.0.0.1:8000/api/',
      // apiUrl: 'http://0.0.0.0:8000/api/',
    // apiUrl: 'http://192.168.1.22:8000/api/',
    // apiUrl: 'http://192.168.0.119:8000/api/',
    // apiUrl: 'http://192.168.43.92:8000/api/'
};
